includes:
  - layer:leadership
  - layer:openstack
  - layer:tls-client
  - interface:tls-certificates
options:
  basic:
    use_venv: True
    include_system_packages: False
repo: https://github.com/openstack/charm-ovn
config:
  deletes:
    - verbose
